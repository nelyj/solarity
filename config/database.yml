defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost
  database: solarity
  username: jlizana
  password:
  template: template0

development: *defaults
production: *defaults
test: *defaults
